[project]
name = "jev-inbox-queue"
version = "0.1.0"
description = "Turn an inbox into a short action queue using Jev (TypeSafe System One)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "typesafe-sdk>=0.7",
    "python-dotenv>=1.0",
    "keyring>=25.7.0",
]
